group a:
  name: "regex"
  data_size:
    max: 1000
    min: 10
    equal: 1000
    enable: false
  action: # MOVE + DEL not allowed
    move: "./core"
    delete: false
    copy: "./core"
  enable: true #boolean
  priority: 1 #some int

group b:
  name: "regex"
  data_size:
    max: 1000
    min: 10
    equal: 1000
    enable: true
  action: # MOVE + DEL not allowed
    move: "./core/cli"
    delete: false
  enable: true #boolean
  priority: 1 #some int
