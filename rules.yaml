group a:
  name: '(?i)ordin'
  extension: '\.go'
  dataSize:
    max: 1000
    min: 10
    equal: 1000
    enable: false
  action: # MOVE + DEL not allowed
    move: "/Users/purushotham"
    delete: false
  enable: true #boolean
  priority: 1 #some int

group b:
  name: "(?i)parser"
  dataSize:
    max: 1000
    min: 10
    equal: 1000
    enable: false
  action: # MOVE + DEL not allowed
    move: "/Users/purushotham"
    delete: false
  enable: true #boolean
  priority: 1 #some int
